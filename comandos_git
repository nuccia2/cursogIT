do Curso
git ini . -> cria um repo git na pasta atua
git status -> mostra o status do trabalho (de cada arquivo)
git add nome_arq -> add arq para ser commitado
git commit -m "mensagem de lembrete" -> faz o commit do que estava para ser commitado
git log -. mostra todo o hotórico

fLUXO DE TRABALHO

1. criar o diretório do projeto
2. iniciar o git dentro do diretório (git init .)
3. edit ou cria arquivos
4. git status.
5. git add (nome do arquivo) ou add . para todos as diretório da pasta
6. git commit -m "mensagem"
7. volta para o passo 3
8. git log ( para ver o que foi comitado)
9. git diff -> o que mudei no ultimo, estado atual
10. git diff . -> para cada arquivo
11. git reset nome -> volta antes do commit


Branch

12. git checkout main ou outra branch -> caminhar pelas branches
13. git branch -D e o nome -> apaga 
a ideia é...trabalhei numa seucundária e depois não preciso mais dele, pois antes eu mesclei, trouxe tudo para a principal por exemplo.
14. gir merge e o nome da branch que quero trazer os arquivosgit 
xxxx

15. git checkout -b secundaria -> cria branch
16. git branch -> lista as branchs


xxx=======================x=======================xxx
## Git fluxo de trabalho LOCAL
1. git clone <end. do projeto>
1.  git checkout -b <nova branch 
2. cria ou atualiza arquivos
3. git status
4. git add .
5 .git status
6. git commit -m "mensagem"
git push origin <nova branch>
abrir pull request no GitHub para main
excluir <novabranch> origin
7. git checkout main
8. git merge nova_branch

## GitHub fluxo de trabalho no LOCAL (projeto próprio ou não)
1. forc do projeto para seu próprio github
1.  git clone <endereço do projeto> 
2. git checkout -b <nova branch>
3. atualiza arquivos
4. git status
4. git add <arquivo>
5 .git status
6. git commit -m "mensagem"
7. git push origin <nova branch>
8. abrir pull request no GitHub para main
10. excluir <novabranch> origin
11 git checkuont main
12 git branch -D <nova branch>
==============================================





Caso especial: branch de outro fork
git remote add upstream https://github.com/USUARIO/REPO.git
git fetch upstream
git switch -c branch_nome upstream/branch_nome



